%section.resume-section.p-3.p-lg-5.d-flex.flex-column
  %h2 Video

  - if @videos.count == 0
    .subheading.mb-5
      ライブ情報がありません。
  - else
    - @videos.each do |v|
      .card.flex-md-row.mb-3
        .card-body
          %string.d-inline-block.mb-2.text-primary
            = v.content_details.video_published_at.strftime("%Y-%m-%d (%a)")
          %h4.mb-1.nowrap-ellipsis
            = v.snippet.title
        %a{class: "youtube", href: "https://www.youtube.com/watch?v=#{v.content_details.video_id}", target: "empty"}
          %img{width: "320", height: "180", src: "https://img.youtube.com/vi/#{v.content_details.video_id}/mqdefault.jpg", class: "card-img-right flex-auto d-lg-block", alt: ""}